---
import "@/styles/default.css";
import Navigation from "@/components/Navigation.astro";
import Footer from "@/components/Footer.astro";

import { ViewTransitions } from "astro:transitions";

export interface Props {
	activePage: string;
	title: string;
}

const { title } = Astro.props;
---

<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta
			name="description"
			content="Rock It! - A music player by Icass & Nicorebo18"
		/>
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/rockit-logo.ico" />
		<meta name="generator" content={Astro.generator} />
		<title>{title}</title>
		<ViewTransitions />
	</head>
	<body>
		<main class="grid grid-rows-[1fr_min-content]">
			<div
				class="grid grid-cols-[min-content_1fr] h-full min-h-0 max-h-full"
			>
				<div class="w-10 bg-black">
					<Navigation />
				</div>
				<div class="min-w-0 max-w-full min-h-0 max-h-full overflow-auto">
					<slot />
				</div>
			</div>
			<Footer />
		</main>
	</body>
</html>
