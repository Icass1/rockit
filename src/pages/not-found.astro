---
import Layout from "@/layouts/Layout.astro";

if (!Astro.locals.user) {
  return Astro.redirect("/login");
}
---

<Layout title="Rock It!" currentPage="404 - Page not Found">
    <div class="h-screen w-full flex items-center justify-center bg-gradient-to-r from-[#fca5a5] to-[#feb2b2] relative">
        <!-- Contenedor de la página -->
        <div class="flex items-center gap-10">
            <!-- Sección de texto -->
            <div class="text-black">
                <h1 class="text-9xl font-bold mb-4">404</h1>
                <p class="text-lg font-semibold mb-2">
                Whoops! Looks like this track is off-key. 🎵
                </p>
                <p class="text-md font-medium">
                We couldn’t find the page you’re looking for, but don’t worry, the music still plays on.
                </p>
            </div>

            <!-- Tocadiscos -->
            <div class="relative w-32 h-32 flex items-center justify-center">
                <!-- Plato del tocadiscos -->
                <div class="absolute w-full h-full rounded-full bg-green-500 animate-spin-slow"></div>
                
                <!-- Disco giratorio -->
                <img
                    src="/vinyl-placeholder.png"
                    alt="Vinyl Record"
                    class="absolute w-4/5 h-4/5 rounded-full object-cover animate-spin-slow"
                />

                <!-- Brazo del tocadiscos -->
                <div class="absolute top-0 right-0 w-16 h-16 bg-blue-500 rotate-12 z-20"></div>
            </div>
        </div>
    </div>
</Layout>

<style>
    /* Animación de giro lento */
    .animate-spin-slow {
        animation: spin 6s linear infinite;
    }

    @keyframes spin {
        0% {
            transform: rotate(0deg);
        }
        100% {
            transform: rotate(360deg);
        }
    }
</style>
