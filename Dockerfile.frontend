# Astro Dockerfile
FROM rockit-frontend-beta-base

WORKDIR /app

# Copy application code
COPY . .

RUN rm .env || true

# RUN chown -R 1000:1000 /app

EXPOSE 3000

RUN mkdir /app/database

RUN pnpm audit

RUN pnpm run build

# Serve the NextJS application
CMD pnpm start
